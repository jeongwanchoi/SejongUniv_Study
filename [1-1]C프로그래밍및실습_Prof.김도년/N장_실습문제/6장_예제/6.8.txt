#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>

int main()
{
	int n;
	int sum = 0;

	scanf("%d", &n);

	do
	{
		int digit = n % 10;
		sum += digit;
		n /= 10;
	} while (n > 0);
	
	printf("%d", sum);

	return 0;
}