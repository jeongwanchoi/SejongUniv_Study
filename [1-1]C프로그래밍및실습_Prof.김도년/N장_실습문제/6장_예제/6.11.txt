#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>

int main()
{
	int odd_sum = 0, even_sum = 0;

	for (int i = 1; i <= 10; i++)
	{
		if (i % 2 == 0)	even_sum += i;
		else	odd_sum += i;
	}
	printf("%d\n%d", odd_sum, even_sum);

	return 0;
}