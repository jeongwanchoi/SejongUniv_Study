#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>

int main()
{
	int n;
	int min = 9999;

	while (1)
	{
		scanf("%d", &n);

		if (n == 0)	break;

		if (min > n)	min = n;
	}
	printf("%d", min);

	return 0;
}