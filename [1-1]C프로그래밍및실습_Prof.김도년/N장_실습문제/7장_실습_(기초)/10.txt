#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>

int main()
{
	double arr[10];
	for (int i = 0; i < 10; i++)	scanf("%lf", &arr[i]);

	for (int i = 0; i < 10; i++)
	{
		if (arr[i] > 0)
		{
			printf("% .1lf", arr[i]);
		}
	}


	return 0;
}