#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>

int main()
{
	int n;
	int sum = 0;
	while (1)
	{
		scanf("%d", &n);
		if (n <= 0)
		{
			printf(" %d", sum);
			break;
		}

		if (sum + n > 20)
		{
			printf(" %d", sum);
			sum = 0;
		}
		sum += n;
	}

	return 0;
}