#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>
#include <math.h>

int main()
{
	int n;
	scanf("%d", &n);

	int hun = round((double)n / 1000) * 1000;
	printf("%d", hun);

	return 0;
}