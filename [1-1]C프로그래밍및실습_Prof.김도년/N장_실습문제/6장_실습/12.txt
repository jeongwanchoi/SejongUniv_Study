#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
   int i = 0, n;
   scanf("%d", &n);

   while (i < n) {
      i += 1;
      for (int j = i ; j<=i+n-1 ; j++ ) {
         printf(" %d ", j % 10);
      }
      printf("\n");
   }
}