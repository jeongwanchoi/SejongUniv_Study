#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>

int main()
{
	int n;
	scanf("%d", &n);

	int edit_n = n;
	int rvs = 0;
	while (edit_n > 0)
	{
		int digit = edit_n % 10;
		rvs = (rvs * 10) + digit;
		edit_n /= 10;
	}

	printf("%d", rvs * rvs);

	return 0;
}